spring:
  application:
    name: config
  profiles:
    active: native

#
#eureka:
#  instance:
#    non-secure-port: ${server.port:8888} #路由器
#    metadata-map: #区域
#      instanceId: ${spring.application.name}:${random.value}#区域id？
#  client:
#    service-url:
#      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/

